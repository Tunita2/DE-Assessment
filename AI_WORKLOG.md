# AI Work Log — Data Engineer Assessment (Xbrain / TechX)

Nhật ký sử dụng AI có kiểm chứng trong quá trình thực hiện POC Data Engineer & Mini Knowledge Base.

> **Nguyên tắc**: 
> - Chọn lọc 8–15 entry có ý nghĩa nhất về mặt kỹ thuật và ra quyết định.
> - Trung thực ghi lại các trường hợp AI đề xuất sai/chưa tối ưu và quá trình kiểm chứng, sửa đổi.
> - Tất cả code/tài liệu đưa vào bài nộp đều được hiểu rõ và kiểm chứng thực tế.

---

## 📋 Bảng Nhật ký AI (AI Work Log)

| STT | Việc (Task) | Prompt | Output & đánh giá | Verify & sửa |
| :---: | :--- | :--- | :--- | :--- |
| **01** | Khởi tạo cấu trúc repository & tài liệu dự án | *"Tạo cấu trúc thư mục repo cho bài POC DE theo đúng tài liệu mô tả Xbrain_Assessment_DE_01_Domain_POC và Bài 2 AI Proficiency"* | **AI trả về:** Đầy đủ các thư mục `pipeline/`, `kb/`, `design/`, `sop/`, `ai_proficiency/`, `README.md`, `AI_WORKLOG.md`.<br>**Đánh giá:** Đúng cấu trúc yêu cầu của đề bài, phân tách rõ ràng giữa code pipeline, KB và báo cáo. | **Cách kiểm chứng:** Đối chiếu với bảng thư mục mục *Nộp bài* trong file đề bài Bài 1 và yêu cầu nộp chung Bài 2.<br>**Đã sửa:** Bổ sung thêm `.gitignore` chuẩn Python và `requirements.txt`. |
| **02** | Xây dựng End-to-End Data Pipeline (Phần A) | *"Trước khi viết pipeline log -> dữ liệu sạch -> báo cáo thì đọc trước các file vận hành data/docs rồi tiến hành code pipeline end to end để xử lý data app_logs_7days.jsonl. Cấu trúc pipeline refactor rõ ràng."* | **AI trả về:** Đọc 8 docs vận hành, xây dựng cấu trúc module hóa `pipeline/src/` (`models`, `ingest`, `validator`, `storage`, `analytics`), xuất dữ liệu Parquet (Snappy), SQLite DB và `quarantine_logs.jsonl` (84 dòng lỗi). Viết 7 unit tests passed.<br>**Đánh giá:** Pipeline chạy đúng luồng, tách biệt rõ ràng các tầng ingest, clean, store và report. | **Cách kiểm chứng:** Chạy `pytest` (7/7 passed) và chạy thực thi `run_pipeline.py` kiểm tra các file artifact sinh ra trong `pipeline/output/`. |
| **03** | Khảo sát Cơ chế hoạt động & Luồng dữ liệu từng giai đoạn | *"Cho tôi xem cơ chế hoạt động của pipeline, input và output của từng giai đoạn"* | **AI trả về:** Sơ đồ luồng dữ liệu 4 giai đoạn, mô tả chi tiết input/output của Ingest, Validator, Storage và Analytics.<br>**Đánh giá:** Chi tiết nhưng gây hiểu nhầm về `RawLogRecord` khiến người dùng tưởng là file vật lý trên đĩa. | **Cách kiểm chứng:** Người dùng hỏi tại sao không thấy file `RawLogRecord` trên ổ cứng.<br>**Đã sửa:** AI làm rõ `RawLogRecord` là In-memory Data Model (trong RAM), chỉ có `clean_logs` và `quarantine_logs` là file vật lý được ghi xuống đĩa. |
| **04** | Đánh giá Phạm vi bắt lỗi & Cơ chế Schema Contract | *"Vậy là dùng rule base cho từng trường hợp, tại sao bạn lại chọn những trường hợp này. Lỡ bắt thiếu trường hợp thì sao?"* | **AI trả về:** Giải thích 4 nhóm lỗi thực tế từ Data Profiling và cơ chế phòng thủ đa tầng. Nhưng dùng sai thuật ngữ: gọi nhầm Fail-Closed/Whitelist là *"Blacklist theo Schema Contract"*.<br>**Đánh giá:** Logic đúng nhưng dùng thuật ngữ tự mâu thuẫn. | **Cách kiểm chứng:** Người dùng phát hiện và phản biện sự tự mâu thuẫn giữa "Blacklist" và "Schema Contract".<br>**Đã sửa:** AI đính chính và làm rõ cơ chế bản chất là **Whitelist (Allowlist) kết hợp Fail-Closed** (mặc định đóng, chỉ cho qua cái đúng). |
| **05** | Làm rõ Cơ chế lưu trữ Quarantine Log & Vận hành Reprocess | *"Quarantine log chỉ lưu những log sai format chứ ko lưu cả log ERR đúng ko? Để cứu/reprocess những quarantine log là làm gì? Nếu file phình to thì quản lý thế nào?"* | **AI trả về:** Phân biệt rõ Data Quality Error vs Operational Error (`level=ERROR` vẫn vào clean log để báo cáo). Giải thích các kỹ thuật reprocess và quản lý bằng Date Partitioning + S3 Lifecycle.<br>**Đánh giá:** Giải thích rõ ràng, đúng chuẩn kiến trúc Data Lake thực tế. | **Cách kiểm chứng:** Người dùng chuẩn hóa lại hiểu biết: Quarantine log lưu dạng jsonl để audit, không làm bẩn bảng sạch, phục vụ debug/drift mà không cần dồn 1 file khổng lồ. |
| **06** | Tinh chỉnh & Nâng cấp 4 Câu hỏi Nghiệp vụ (Metrics & Baseline) | *"Chất vấn 4 câu hỏi: Q1 tính error rate chưa? Q2 đột biến so với baseline nào, có dùng ngưỡng GUIDE-01 không? Q3 GROUP BY có bị vỡ do message động không? Đã verify chéo Pandas vs SQL chưa?"* | **AI trả về:** Tính bổ sung Error Rate (Q1: 21.38%), tính Baseline ngày thường (Q2: 24.5 lỗi/ngày $\rightarrow$ ngày 30/07 gấp 5.71 lần, đối chiếu ngưỡng CRITICAL > 5% trong GUIDE-01), kiểm chứng message template tĩnh chiếm 79.1% (Q3) và tích hợp hàm `verify_pandas_vs_sql()` khớp 100%.<br>**Đánh giá:** Bổ sung đầy đủ góc nhìn tỷ lệ lỗi nội tại và đối chiếu chéo chuẩn xác giữa 2 engine. | **Cách kiểm chứng:** Người dùng yêu cầu kiểm tra kỹ cơ chế đối chiếu chéo Pandas vs SQL và bám sát tài liệu vận hành GUIDE-01.<br>**Đã sửa:** Hoàn thiện hàm `verify_pandas_vs_sql()` trong `analytics.py`, bổ sung test case trong `test_pipeline.py` và cập nhật đồng bộ các chỉ số vào báo cáo `report_answers.md`. |
| **07** | Thiết kế Kiến trúc Pipeline trên AWS (Phần A — Mục 4) | *"Thiết kế tài liệu kiến trúc AWS cho pipeline xử lý log bám sát đúng scale đề bài (~3K log/ngày), phân tách rõ thành 2 sơ đồ riêng biệt: Kiến trúc hiện tại Lean Serverless (S3, EventBridge, Lambda Python query DuckDB in-process, SNS) và Hướng mở rộng Phase 2 (Glue Crawler, Data Catalog, Athena). Đồng thời nêu rõ bảng ngưỡng chuyển đổi độc lập giữa tầng Compute và Query khi dữ liệu tăng trưởng."* | **AI trả về:** Bản thiết kế tại `design/aws_architecture.md` với 2 sơ đồ Mermaid trực quan, thuyết minh chi tiết luồng xử lý và bảng ngưỡng chuyển đổi.<br>**Đánh giá:** Ban đầu AI đưa cụm Spark/Glue và Athena vào kiến trúc hiện tại gây over-engineering và gặp lỗi cú pháp render Mermaid; sau đó đã được tinh chỉnh loại bỏ service thừa và sửa lỗi cú pháp hoàn chỉnh. | **Cách kiểm chứng:** Người dùng chỉ ra việc đưa Glue/Athena vào scale 3K log/ngày là lãng phí chi phí, yêu cầu tách thành 2 sơ đồ độc lập và sửa lỗi cú pháp Mermaid.<br>**Đã sửa:** AI cập nhật sơ đồ chính thành Lambda xử lý + DuckDB in-process (chi phí $0/tháng), đưa Glue/Athena sang sơ đồ mở rộng Phase 2 với trigger riêng (01:15 AM), và bổ sung bảng tiêu chí chuyển đổi độc lập giữa Compute (>500MB–1GB/ngày) và Query (đa phòng ban phân tích ad-hoc). |



---

## 🔍 Ghi chú & Đúc kết kinh nghiệm sử dụng AI
- **Verification First**: Luôn đối chiếu code được sinh với dữ liệu thực tế (`data/app_logs_7days.jsonl` và `data/docs/`) trước khi chấp nhận.
- **Edge cases**: AI thường giả định dữ liệu sạch hoàn hảo; cần chủ động yêu cầu xử lý malformed JSON, missing fields, timestamp parsing errors.
